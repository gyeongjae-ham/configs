version: '3.8'

services:

  postgres:
    image: postgres:16-bookworm
    container_name: postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: luca
      POSTGRES_PASSWORD: slfl4861
    stdin_open: true
    tty: true
    ports:
      - 5432:5432
    volumes:
      - /Users/luca/Desktop/.util/db/postgres/data:/var/lib/postgresql/data

